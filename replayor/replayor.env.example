# Replayor Configuration Example
# Copy this file to replayor.env and modify the values according to your setup

# Path to the replayor binary
# Default: replayor (assumes it's in PATH or built locally)
REPLAYOR_BINARY=replayor

# Enable continuous mode (true/false)
CONTINUOUS_MODE=true

# Engine API JWT secret (hex string)
# This should match the JWT secret used by your execution client
# Default: example secret (change in production!)
ENGINE_API_SECRET=0c00f14247582fcd3c837311148cda1f56e7c2caa42fb1ba8a3cc7843603846e

# Engine API URL (authenticated RPC endpoint)
# Default: http://127.0.0.1:8552
ENGINE_API_URL=http://127.0.0.1:8552

# Execution client URL (HTTP RPC endpoint)
# Default: http://127.0.0.1:8123
EXECUTION_URL=http://127.0.0.1:8123

# Source node URL (where to fetch blocks from)
# This is typically an RPC endpoint of a synced node
SOURCE_NODE_URL=https://rpc.xlayer.tech

# Strategy to use for replaying blocks
# Options: replay, stress, erc20
# Default: replay
STRATEGY=replay

# Path to rollup configuration file
# For L2s like Optimism, this contains the rollup parameters
# Default: ./rollup.json
ROLLUP_CONFIG_PATH=./rollup.json

# Path to store benchmark results
# Default: ./result
DISK_PATH=./result

# Storage type for results
# Options: disk, s3
# Default: disk
STORAGE_TYPE=disk

# Number of blocks to process
# Default: 1
BLOCK_COUNT=1

# Benchmark start block (optional)
# If set, replayor will first fast-forward to this block before starting benchmark
# Useful when starting from a snapshot: set to snapshot_block_number + 1
# Example: If snapshot is at block 1000000, set to 1000001
# Default: 0 (disabled, start from current block)
#BENCHMARK_START_BLOCK=0

