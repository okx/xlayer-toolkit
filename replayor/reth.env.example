# Reth Node Configuration Example
# Copy this file to reth.env and modify the values according to your setup

# Path to the reth binary (op-reth or reth)
# Default: op-reth (assumes it's in PATH)
RETH_BINARY=op-reth

# Data directory for reth node
# Default: ./reth-data
RETH_DATA_DIR=./reth-data

# Chain configuration file (genesis.json)
# Default: ./genesis.json
RETH_CHAIN=genesis.json

# JWT secret file for authenticated RPC
# Default: ./jwt.txt
RETH_JWT_SECRET=./jwt.txt

# HTTP RPC port
# Default: 9123
RETH_HTTP_PORT=9123

# WebSocket RPC port
# Default: 9124
RETH_WS_PORT=9124

# Authenticated RPC port (for consensus client communication)
# Default: 8553
RETH_AUTHRPC_PORT=8553

# P2P networking port
# Default: 50505
RETH_P2P_PORT=50505

# Verbosity level (-v, -vv, -vvv, -vvvv)
# Default: -vvvv
RETH_VERBOSITY=-vvvv

# HTTP API modules (comma-separated)
# Default: web3,debug,eth,txpool,net,miner,admin
RETH_HTTP_API=web3,debug,eth,txpool,net,miner,admin

# WebSocket API modules (comma-separated)
# Default: web3,debug,eth,txpool,net
RETH_WS_API=web3,debug,eth,txpool,net

# Unwind target block (optional, used by unwind.sh)
# You can also pass the block number as a command line argument to unwind.sh
# Default: 8594000
# UNWIND_TO_BLOCK=8594000

