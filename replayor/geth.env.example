# Geth Node Configuration Example
# Copy this file to geth.env and modify the values according to your setup

# Path to the geth binary (op-geth or geth)
# Default: geth (assumes it's in PATH)
GETH_BINARY=geth

# Data directory for geth node
# Default: ./geth-data
GETH_DATA_DIR=./geth-data

# JWT secret file for authenticated RPC
# Default: ./jwt.txt
GETH_JWT_SECRET=./jwt.txt

# HTTP RPC port
# Default: 9123
GETH_HTTP_PORT=9123

# WebSocket RPC port
# Default: 9124
GETH_WS_PORT=9124

# Authenticated RPC port (for consensus client communication)
# Default: 8553
GETH_AUTHRPC_PORT=8553

# P2P networking port
# Default: 50505
GETH_P2P_PORT=50505

# Verbosity level (0-5, where 5 is most verbose)
# Default: 4
GETH_VERBOSITY=4

# HTTP API modules (comma-separated)
# Default: web3,debug,eth,txpool,net,miner,admin
GETH_HTTP_API=web3,debug,eth,txpool,net,miner,admin

# WebSocket API modules (comma-separated)
# Default: web3,debug,eth,txpool,net
GETH_WS_API=web3,debug,eth,txpool,net

# Transaction pool configuration
# Default: 100000
GETH_TXPOOL_ACCOUNTSLOTS=100000
GETH_TXPOOL_GLOBALSLOTS=100000
GETH_TXPOOL_GLOBALQUEUE=100000

