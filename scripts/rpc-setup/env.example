# User Configuration (Fill these in)
L1_RPC_URL={your-l1-url}
L1_BEACON_URL={your-l1-beacon-url}

# Network Type: testnet or mainnet (default: testnet)
NETWORK_TYPE=testnet

# L2 Engine Type: geth or reth (default: geth)
# Note: Docker Compose profiles will be set automatically based on this value
L2_ENGINEKIND=geth

# Data Directory Root (default: chaindata)
# All blockchain data will be stored under: chaindata/{network}-{engine}/
CHAIN_DATA_ROOT=chaindata

# Port Configuration (optional, will use defaults if not set)
RPC_PORT=8545
WS_PORT=7546
NODE_RPC_PORT=9545
GETH_P2P_PORT=30303
NODE_P2P_PORT=9223

# Docker Image Tags (will be set automatically based on network type if not specified)
# OP_STACK_IMAGE_TAG=xlayer/op-node:0.0.9
# OP_GETH_IMAGE_TAG=xlayer/op-geth:0.0.6
# OP_RETH_IMAGE_TAG=xlayer/op-reth:release-testnet
