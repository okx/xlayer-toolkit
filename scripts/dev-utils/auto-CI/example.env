# Configuration
# Path to the optimism repository (REQUIRED)
REPO_DIR=/data1/brendon/xlayer-repos/optimism

# Upstream repository to pull from
UPSTREAM_REPO=https://github.com/okx/optimism.git

# Branch to pull from upstream
UPSTREAM_BRANCH=brendon/fix-optimism-CI

# Contracts directory (relative to REPO_DIR)
CONTRACTS_DIR=packages/contracts-bedrock

# Test Configuration
# Filter to run specific tests (e.g., TestBatcherAutoDA)
# Leave empty to run all tests
TEST_FILTER=

# Notification Configuration
# Lark webhook URL for notifications
# Leave empty to disable notifications
LARK_WEBHOOK_URL=

# Build Configuration
# Force rebuild of fraud proof binaries even if they exist
FORCE_REBUILD=false 