# X Layer Prestate Build Script

## Background
Unified build script for X Layer prestate generation with:


## Usage

### Simple: Copy and Run

```bash
# 1. Copy script to optimism root
cp build-prestate.sh /path/to/optimism/
cd /path/to/optimism

# 2. Run build
chmod +x build-prestate.sh

# For testnet
./build-prestate.sh testnet

# For mainnet
./build-prestate.sh mainnet
```
